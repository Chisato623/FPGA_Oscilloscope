//按键消抖模块
module key_filter(
    input Clk       ,
    input Reset_n   ,                           //~i_rst
    input Key       ,
   output Key_P_Flag    // this is the flag which means the key was pressed
);

endmodule
